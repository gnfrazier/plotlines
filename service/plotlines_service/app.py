# Stub — no endpoints yet.
# Endpoint surface (ARCH §7.2): routing (both modes), content (both modes, cache-backed),
# accounts (hosted mode only), and group relay (P9, hosted mode only, trip-scoped).
# Sidecar lifecycle (spawn/health/shutdown) is ARCH §7.3 — not implemented in this scaffold.
