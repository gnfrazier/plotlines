"""Interfaces only — see ARCH §13.2.

Pluggable data-source Protocol interfaces (EdgeDataProvider, NodeDataProvider,
ShapeDataProvider, WaterwayDataProvider). Core ships zero implementations
beyond OSM defaults.
"""
