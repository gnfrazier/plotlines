"""Curation tier (ARCH §4.2-§4.3) — layer taxonomy, notability filtering and
salience scoring, and mode×day-type layer defaults. PRD FR97-FR99, Story N3.

Candidates produced here are not canon (ARCH P10): nothing in this package
writes into `trip.payload`. It scores what a bbox's raw features are worth
looking at; promotion is an Author action that lives above this package.
"""
